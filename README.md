# Hyperdrive-Perception
Hyperdrive Perception is a real-time object detection system for ADAS using a lightweight YOLOv11n model trained on the KITTI dataset. It detects vehicles and pedestrians with low latency (~30 FPS) and high accuracy. Optimized with ONNX, it runs efficiently on edge devices without cloud dependency.
